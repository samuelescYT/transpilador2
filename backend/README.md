# Backend del Transpilador Python → Java
Ejecuta un servidor Flask que recibe código Python y devuelve el código Java traducido.

## Instalación
```bash
cd backend
pip install -r requirements.txt
python app.py
